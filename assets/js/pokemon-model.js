// Criando os pokemons com os dados que precisamos
// Para nao ter que ficar pegando da API
// Pois ela possui muitos dados, e vamos usar poucos
class Pokemon{
    number;
    name;
    type;
    types = [];
    photo;
}